# 제주 원도심 아트맵 작업 지침

- 실제 구현은 `app/`과 `worker/` 코드를 최종 기준으로 판단한다.
- 공개 지도, 관리자 편집, PWA 설치 환경을 함께 보존한다.
- 지도 좌표·자산 경로·DB 저장 형식은 요청 없이 변경하지 않는다.
- 모바일 조작 성능을 우선하되 정지 화면의 이미지 선명도를 낮추지 않는다.
- 변경 후 `npm run lint`와 `node --test tests/*.test.mjs`를 실행한다.
- 배포 전 Sites 체크포인트의 프로덕션 빌드 검증을 통과해야 한다.
- 현재 기능과 다음 검토 항목은 `PROJECT_STATE.md`, 구조는 `ARCHITECTURE.md`를 먼저 확인한다.

