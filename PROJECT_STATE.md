# 현재 상태

## 구현됨

- 공개 지도와 관리자 편집 화면
- 장소 상세, 후기·사진 업로드, 행사, 장소 등록 요청
- PWA 설치·업데이트·캐시 분리
- 모바일 핀치·드래그 합성 렌더링과 성능 진단 기록
- 관리자 진단·DB 편집 UI 지연 로딩
- EVENT, NEW·후기 수 라벨 표시와 추가분류 기반 교차 노출
- 패널 닫기 시 현재 지도 축척·위치 보존
- 로딩 종료 전 주요 거점 확대·위치·라벨 구성의 실제 화면 반영 확인
- 관리자·공개 지도 모두 첫 입력 전부터 중심 기준 변환을 적용
- 장소 이동·핀치·휠 전환은 합성 변형만 애니메이션하고 실제 지도 폭은 종료 시 한 번만 확정
- 확대 종료 후 라벨 수량·밀집·위치 계산은 지도 폭 확정과 분리된 낮은 우선순위 갱신으로 처리
- 모바일 PWA의 초기 주요 거점 맞춤은 목표 폭·위치를 DOM에 직접 동기화한 뒤 로딩을 종료
- PWA 캐시 이미지가 즉시 완료돼도 시작 진행률을 먼저 초기화하고 완료 시 전체 수량으로 확정
- 지도 드래그·핀치·휠·장소 이동 중에는 라벨과 통합 라벨 연결선을 그리지 않고 최종 위치 확정 뒤 복구
- 조작 종료 후 라벨은 짧은 투명도 전환으로 복구하되 이동 시작 시에는 즉시 페인트 경로에서 제외
- 조작 종료 후 라벨 복구 시간은 0.28초이며 EVENT·NEW·후기 수는 장소명과 겹치지 않는 라벨 바깥 상단 줄에 표시
- 모바일 베이스맵은 확대 단계별 소스 교체 없이 고화질 화면용 이미지 한 장을 유지
- 공개 모바일 지도는 현재 화면 주변에 넉넉한 여유 범위를 두고 먼 화면 밖 마커·통합 라벨 DOM만 제외
- 공개 장소 상세와 그 안의 행사·후기 화면은 장소를 열 때만 별도 UI 묶음을 불러옴
- 모바일 장소·탐색 패널은 높이 애니메이션 없이 최종 높이를 한 번 확정하고 합성 변환으로 짧게 안착
- 모바일 장소·탐색 패널의 배경 블러를 제거하고 라벨·패널·탭 전환 시간을 공통 모션 기준으로 정리
- 전체 원도심 탐색의 리뷰·행사 카드 UI는 해당 탭을 열 때만 별도 묶음으로 불러오고 짧게 교차 표시
- PC 지도는 가운데 정렬을 레이아웃에 맡기고 제스처 중 순수 `scale3d`만 적용해, 정렬 이동과 확대가 한 변환에서 소수점 반올림되며 마커·랜드마크가 좌우로 흔들리던 경로를 분리
- 모바일 지도는 검증된 기존 변환 경로를 유지하고, 화면 밖 요소 여유 범위를 기기 메모리·코어 수에 따라 3단계로 조정
- 모바일 조작 종료가 80ms 이상 걸리는 상황이 두 번 누적되면 현재 세션의 화면 밖 렌더 여유 범위를 저사양 단계로 자동 축소

## 중요 제약

- 정지 화면의 지도·랜드마크 화질을 낮추지 않는다.
- 전체 지도 복귀는 명시적인 `전체 지도` 동작에서만 수행한다.
- 브라우저 뒤로가기는 기존 탐색 이력을 유지한다.
- 라벨 구성에는 확대 종료 시 크기 보간을 덧씌우지 않고, 복구 가시성에만 짧은 투명도 전환을 사용한다.
- 확대 애니메이션에서 지도 `width`를 프레임마다 변경하지 않는다.
- 목표 확대·위치를 두 프레임 반영한 뒤에는 기기 픽셀 반올림값으로 로딩 종료를 다시 차단하지 않는다.
- 단순 탭에서는 라벨을 숨기지 않고 실제 지도 이동이나 확대·축소가 시작될 때만 숨긴다.
- PC와 모바일의 지도 중심 변환은 분리하되 지도 자산·좌표·정지 화질은 같은 원본을 유지한다.

## 다음 검토

- 실제 저사양 PWA 성능 기록에 따른 80ms 기준과 기기별 화면 밖 요소 여유 범위 미세 조정
- 관리자 전용 행사·장소 요청 폼을 추가 지연 로딩해 공개 첫 화면의 대형 `page.tsx` 파싱량 축소
