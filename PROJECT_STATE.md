# 현재 상태

## 구현됨

- 공개 지도와 관리자 편집 화면
- 장소 상세, 후기·사진 업로드, 행사, 장소 등록 요청
- PWA 설치·업데이트·캐시 분리
- 모바일 핀치·드래그 합성 렌더링과 성능 진단 기록
- 관리자 진단·DB 편집 UI 지연 로딩
- EVENT, NEW·후기 수 라벨 표시와 추가분류 기반 교차 노출
- 패널 닫기 시 현재 지도 축척·위치 보존
- 로딩 종료 전 주요 거점 확대·위치·라벨 구성의 실제 화면 반영 확인
- 관리자 PC 지도는 기존 직접 상태 렌더 방식으로 복원해 이동·휠 확대를 하나의 wrapper 변환에 즉시 반영하고 합성 렌더 전환과 시점 안전영역 보정을 사용하지 않음
- 관리자 맞춤 축척은 가로·세로가 모두 보이는 초기 계산을 사용하고 기본적으로 명시적 라벨 가시성 설정을 그대로 따르되, 화면 설정의 체크를 켜면 저장된 배포본 축척 단계별 전체 라벨 목표 수를 관리자 지도에서 미리 적용
- 공개 장소 이동·핀치·휠 전환은 합성 변형만 애니메이션하고 실제 지도 폭은 종료 시 한 번만 확정
- 공개 확대 종료 후 다음 프레임에 라벨 구성·통합 라벨·상태 정보를 함께 복구
- 모바일 PWA의 초기 주요 거점 맞춤은 목표 폭·위치를 DOM에 직접 동기화한 뒤 로딩을 종료
- PWA 캐시 이미지가 즉시 완료돼도 시작 진행률을 먼저 초기화하고 완료 시 전체 수량으로 확정
- 공개 지도 드래그·핀치·휠·장소 이동 중에는 라벨과 통합 라벨 연결선을 그리지 않고 최종 위치 확정 뒤 복구
- 공개 조작 종료 후 라벨은 짧은 투명도 전환으로 복구하되 이동 시작 시에는 즉시 페인트 경로에서 제외
- 공개 조작 종료 후 라벨 복구 시간은 0.12초이며 EVENT·NEW·후기 수는 장소명과 겹치지 않는 라벨 바깥 상단 줄에 표시
- 저사양 모바일 베이스맵은 일반 화면에서 2048 화면용 이미지를 사용하고 실제 표시 픽셀 수가 선명도 임계치에 가까워질 때 4096 이미지를 낮은 우선순위로 미리 디코딩한 뒤 세션 중 한 번만 승격하며, 표준·고사양 모바일과 PC는 4096 화면용 이미지를 유지
- 공개 모바일 랜드마크는 최초 화면 프로필에서 고정 384px WebP 파생본을 선택하고 확대율·기기 등급 변화에 따른 128·256·512px 런타임 교체 없이 세션 내 같은 자산을 유지; 관리자·PC 화면은 기존 512px 화면본, 출력은 고해상도 원본 유지
- 공개 카테고리 UI와 재단 로딩·헤더 CI는 기존 PNG와 픽셀값이 같은 무손실 WebP를 사용하고 범용 마커 SVG·PWA 설치 PNG는 기존 형식 유지
- 최신 v20 고해상도 출력 지도는 원본 PNG와 전체 픽셀값이 같은 8,944×7,324 무손실 WebP를 버전 고정 경로로 사용하고, 최종 내보내기 파일은 기존처럼 PNG로 생성; 원본 PNG와 향후 관리자 지도 업로드 경로는 보존
- 공개 모바일 지도는 현재 화면 주변에 기기별 여유 범위를 두고 먼 화면 밖 마커·통합 라벨 DOM만 제외하며, 저사양 단계는 35%·최소 72px 범위를 사용
- 공개 장소 상세와 그 안의 행사·후기 화면은 장소를 열 때만 별도 UI 묶음을 불러옴
- 모바일 장소·탐색 패널은 높이 애니메이션 없이 최종 높이를 한 번 확정하고 합성 변환으로 짧게 안착
- 모바일 장소·탐색 패널의 배경 블러를 제거하고 라벨·패널·탭 전환 시간을 공통 모션 기준으로 정리
- 전체 원도심 탐색의 리뷰·행사 카드 UI는 해당 탭을 열 때만 별도 묶음으로 불러오고 짧게 교차 표시
- 공개 PC 지도는 가운데 정렬을 레이아웃에 맡기고 제스처 중 순수 `scale3d`만 적용하며, 관리자 PC 지도는 별도의 기존 직접 렌더 경로를 사용
- 모바일 지도는 검증된 기존 변환 경로를 유지하고, 화면 밖 요소 여유 범위를 기기 메모리·코어 수에 따라 3단계로 조정
- 모바일 조작 종료가 80ms 이상 걸리는 상황이 두 번 누적되면 현재 세션의 화면 밖 렌더 여유 범위를 저사양 단계로 자동 축소
- 성능 진단은 시작 시간을 세션당 한 번 기록하고 이동·확대 정착은 5회 중 1회만 표본화하되 200ms 이상 지연은 항상 기록
- 모바일 웹·PWA는 축척과 기기 성능 단계에 따라 실제 이미지 마커·라벨 수를 제한하고, 추천 여부가 아닌 현재 화면 중심 거리와 축척을 기준으로 일관되게 표시
- 첫 진입 주요 거점 확대 수준에서는 현재 화면 여유 범위 안의 마커를 생략하지 않고, 전체 지도에 가까워질수록 저·표준·고사양별 마커 예산을 단계적으로 강화
- 맞춤 축척의 1.45배 이하 전경에서는 추천·주요 거점·선택 여부와 관계없이 일반 장소를 모두 카테고리 원으로 통일하고 실제 마커는 랜드마크만 유지
- 모바일에서 제한된 일반 마커는 실제 마커보다 작은 화면 기준 8px 카테고리 원으로 고정하고, 확대·축소 제스처 중에는 지도 변형의 역배율을 원형 계층 한 곳에만 적용해 화면 크기가 불어나지 않도록 유지하며, 랜드마크는 화면 밖 요소 제한과 마커 수 제한에서 항상 제외
- 간략 원은 지도 전경을 덜 가리는 0.75px 어두운 회색 테두리를 사용하고, 간략 원·분류 표시·공개 카테고리 UI는 승인 마커 바깥 원 색상을 공통 원본으로 사용하며 음식점은 `#E36B58`의 붉은색을 유지
- 공개 라벨은 마커와 별도 예산을 사용하고, 맞춤 축척 단계값을 랜드마크·주요 거점·현재 선택까지 포함한 화면 전체 라벨 목표 수로 적용해 지도 전경을 단순하게 유지
- 맞춤 축척 2.5배를 넘으면 전체 라벨 목표 수를 단계적으로 늘리고 4.5배를 넘는 상세 화면에서만 현재 표시 대상 라벨 전체를 복구하며, 필수 라벨 수가 목표보다 많을 때만 필수 라벨 전체를 우선 유지
- 공개 개별 라벨 글자는 통합 라벨과 같은 화면 기준 8px로 고정하고, 통합 라벨 연결선은 거리와 관계없이 불투명도 0.84로 유지하면서 최대 1.5px·선택 연결선은 1.55px로 제한하되 관리자 글자·연결선 규칙은 유지
- 추천 여부는 화면 렌더 우선순위에 관여하지 않으며, 제한 구간의 선택은 랜드마크·현재 선택·주요 거점 보존 후 화면 중심 거리와 배치 순서를 따름
- 지도 축소 하한은 베이스맵의 좌우 끝이 화면 여백에 맞는 배율로 통일해 버튼·단축키·휠·핀치에서 더 축소되지 않음
- 모바일 확대 종료 시 새 라벨 구성이 확정된 뒤 한 번에 복구되도록 공개 시점을 맞춰 이전·신규 라벨이 순차적으로 나타나는 현상 완화
- PWA 독립 실행 모드의 최상위 배경을 밝은색으로 고정하고 상단 안전영역만 차콜색으로 유지해 합성 레이어 재구성 중 전체 암전 방지
- 관리자 행사 등록·수정 창은 실제로 열 때만 별도 UI 묶음을 불러옴
- 관리자 PC 휠 확대는 매 입력 시점의 실제 마우스 위치와 현재 지도 상태를 바로 사용해 종료 후 좌표계 재확정으로 생기던 좌우 밀림을 제거
- 공개 장소 상세에서 권역 분류를 숨기고 DB 설명·운영정보·비고/주의사항을 별도 상자 없이 한 정보 흐름으로 표시
- 배포본 원도심 탐색은 `전체 항목 수`와 검색창을 한 줄에 배치하고 결과 안내 행과 카테고리 상단 여백을 줄여 카테고리·세부 목록을 위로 이동
- 관리자 `화면·라벨 표시 설정`에서 맞춤 축척 1.25·1.7·2.1·2.5·3·3.6·4.5배의 전체 라벨 목표 수를 직접 조절하며, 랜드마크·주요 거점·현재 선택도 이 수에 포함
- 관리자 통합 라벨 연결선은 고정 화면 크기로 복원된 라벨과 같은 역배율 좌표계를 사용해 라벨 행과 선 끝점의 축척별 오차를 방지
- 관리자 개별·통합 라벨은 8월 10일 전후의 역배율 방식을 관리자 경로에만 복원해 확대율과 무관한 9.5px·8.5px 화면 크기를 유지
- 관리자 지도 이동·축척 변경 중에는 개별·통합 라벨과 통합 연결선을 숨기고 종료 150ms 후 0.28초 페이드인으로 복구
- 전체 라벨 새로고침은 통합 라벨 수동 위치도 초기화해 주변 이미지·개별 라벨·연결선을 피하는 자동 배치를 다시 적용하며, 통합 라벨이 마커 왼쪽에 놓인 행은 분류 표시점을 오른쪽으로 이동
- 공개 통합 라벨은 드래그·축척 종료 후 현재 화면 경계와 실제 화면상 라벨 폭을 다시 계산해 보이는 지도 안쪽으로 재배치
- 모바일 공개 통합 라벨은 한 묶음 최대 10곳의 한 열 세로 목록으로 표시하고, 관리자 통합 라벨도 좌우 방향과 관계없이 한 열 목록으로 통일
- 관리자 통합 라벨은 항목명 기준의 간결한 한 열 폭을 사용하고, 좌측 배치에서는 항목명을 왼쪽 정렬한 채 분류 표시점과 연결선 끝점을 행 오른쪽에 맞춤
- 관리자 화면·라벨 설정에 현재 개별 라벨 수와 통합 묶음 수를 실시간 표시
- 관리자 화면·라벨 설정에 `관리자 지도에 축척별 라벨 수 적용` 체크를 추가해 배포본 단계값을 실제 편집 지도에서 선택적으로 미리 확인
- 관리자 축척별 전체 라벨 설정은 기본값 버튼 옆 저장 버튼으로 독립 서버 설정에 보관하며, 장소·좌표·자산 공개본을 업데이트하지 않아도 이후 공개 지도 로드부터 즉시 적용
- 통합 라벨 후보 위치 점수 계산은 중간 배열 생성과 전체 정렬 없이 단일 순회로 최솟값을 선택하고, 지도 요소·DB 장소·요청 마커 조회는 메모된 인덱스를 재사용
- 모바일 실제 마커와 간략 원 분리는 후보 배열을 한 번만 순회하며, 공개 장소 검색용 추가분류·편의정보 문자열은 장소 데이터 변경 시에만 미리 계산
- 관리자 장소 등록 요청 카드 UI는 해당 관리 탭을 열 때만 별도 화면 묶음으로 불러옴
- 연결된 화면이 없던 이전 DB 가져오기·직전본 복원 콜백과 표시되지 않는 파생 목록·revision 상태 갱신을 제거해 초기 화면 코드와 불필요한 재렌더를 축소하고, 공개 이력 불러오기와 서버 저장·게시 경로는 유지
- 공개·관리자 하단 상태표시는 현재 맞춤 대비 배율, 선형 지도 표시 비율, 랜드마크·주요 거점·일반 장소를 모두 포함한 실제 출력 라벨 수를 공통 표시
- 실제 지도 라벨 후보와 축척별 상한은 현재 화면 안에 좌표가 들어온 장소만 대상으로 계산하고, 드래그·확대·축소가 끝난 뒤 확정된 위치와 배율로 한 번 갱신
- 공개 하단 라벨 수 오른쪽의 작은 새로고침 버튼은 페이지·지도 자산을 다시 불러오지 않고 현재 화면의 라벨·마커 표시 계산만 다시 실행

## 중요 제약

- 정지 화면의 지도·랜드마크 화질을 낮추지 않는다.
- 전체 지도 복귀는 명시적인 `전체 지도` 동작에서만 수행한다.
- 브라우저 뒤로가기는 기존 탐색 이력을 유지한다.
- 공개 라벨 구성에는 확대 종료 시 크기 보간을 덧씌우지 않고, 복구 가시성에만 짧은 투명도 전환을 사용한다.
- 공개 확대 애니메이션에서 지도 `width`를 프레임마다 변경하지 않는다. 관리자 PC는 기존 wrapper 직접 확대를 사용한다.
- 목표 확대·위치를 두 프레임 반영한 뒤에는 기기 픽셀 반올림값으로 로딩 종료를 다시 차단하지 않는다.
- 공개 단순 탭에서는 라벨을 숨기지 않고 실제 지도 이동이나 확대·축소가 시작될 때만 숨긴다. 관리자도 지도 이동·축척 변경 중에는 라벨을 숨기되 마커·라벨 자체 편집 동작은 유지한다.
- 관리자 PC 직접 렌더와 공개 PC·모바일 합성 렌더를 분리하되 지도 자산·좌표·정지 화질은 같은 원본을 유지한다.
- 모바일 마커 제한은 일반 장소에만 적용한다. 상세 축척의 선택 장소는 실제 마커로 유지하지만, 전경 간략화 구간에서는 선택 장소도 원으로 통일하고 랜드마크만 실제 자산으로 유지한다.
- 관리자에서 명시적으로 숨긴 일반 마커·라벨은 상세 확대에서도 자동으로 되살리지 않되, 공개본 랜드마크 라벨은 축척과 관리자 화면 표시 상태에 관계없이 항상 출력한다.
- 모바일 랜드마크 파생본은 384px 캔버스 비율·투명 경계·앵커 여백을 유지하며, 확대 중 다른 해상도로 승격하거나 강등하지 않는다.

## 다음 검토

- 배포 후 새 저사양 PWA 기록에서 시작·이동·확대 중앙값과 200ms 이상 지연 비율을 이전 기준과 비교
- 실기기에서 일반 배율 2048 선명도, 상세 확대 4096 승격 시 깜빡임, 빠른 이동 가장자리 마커 재진입을 우선 검증
- 실기기에서 384px 랜드마크의 칠성로 간판·목관아/관덕정 기와·소통협력센터 외벽 선명도와 초기 디코딩 감소 효과를 확인
- 새 기록이 모이기 전에는 지도 합성 구조와 시각 효과를 추가로 축소하지 않음
