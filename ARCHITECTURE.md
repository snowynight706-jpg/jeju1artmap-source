# 프로젝트 구조

- `app/page.tsx`: 공개 지도, 관리자 편집기, 지도 조작, 장소 상세·후기·행사 UI의 중심 상태와 화면
- `app/public-place-detail-content.tsx`: 장소 선택 후에만 지연 로딩되는 공개 상세·행사·후기 화면
- `app/public-explorer-activity-content.tsx`: 원도심 탐색에서 리뷰·행사 탭을 열 때만 지연 로딩되는 카드 화면
- `app/globals.css`: 지도·패널·모바일 PWA 반응형 스타일
- `app/admin-*.tsx`: 지연 로딩되는 관리자 전용 화면
- `app/admin-place-event-dialog.tsx`: 관리자 행사 등록·수정 시에만 불러오는 행사 입력 창
- `app/api/`: 공개 배치, 장소 DB, 행사, 후기, 사진, 진단 기록 API
- `app/*density*.mjs`, `app/*placement*.mjs`: 라벨 밀집도·배치 계산
- `app/map-stage-transform.mjs`: PC·모바일별 지도 중심 정렬과 제스처 확대 변환 정책
- `app/mobile-render-budget.mjs`: 기기 성능에 따른 모바일 화면 밖 렌더 여유 범위 단계
- `app/mobile-marker-density.mjs`: 모바일 축척·성능 단계별 실제 마커·라벨 예산과 추천 장소 우선순위
- `public/`: 지도·랜드마크·마커 자산과 PWA manifest/service worker
- `db/`, `drizzle/`: 영구 저장 스키마와 마이그레이션
- `worker/`: 배포 Worker 진입점과 정적 자산 캐시 정책
- `tests/`: 기능·회귀·PWA·성능 정적 검증

지도 조작은 제스처 중 합성 변형을 사용하고, 실제 이동이 시작되면 라벨 페인트를 잠시 중단한 뒤 종료 후 실제 확대율과 라벨 배치를 한 번 확정한다. PC는 지도판의 가운데 정렬을 레이아웃으로 고정하고 제스처 중 확대만 `scale3d`로 처리하며, 모바일은 기존 가운데 이동을 포함한 변환 경로를 유지한다. 라벨은 새 구성이 정착된 뒤 투명도 전환으로 한 번에 복구하고 상태표시는 장소명 바깥 상단 줄에 둔다. 공개 모바일 렌더 계층은 현재 화면의 기기별 여유 범위 안에서 축척·성능 단계만큼 실제 마커를 표시한다. 첫 진입 주요 거점 확대 수준에서는 후보 마커를 모두 유지한다. 맞춤 축척의 1.45배 이하 전경에서는 추천 여부와 관계없이 일반 장소를 모두 12px 카테고리 원으로 통일하고 랜드마크 라벨만 유지한다. 그보다 확대되면 추천 장소부터 실제 마커 예산을 배정하고 일반 라벨을 단계적으로 늘린다. 랜드마크와 선택 장소는 마커 제한에서 제외한다. 느린 정착 시간이 반복되면 현재 세션에서 저사양 범위와 마커·라벨 예산을 함께 적용한다. 장소 목록과 데이터 계산은 전체 공개 요소를 계속 사용하며, 선택 장소 상세, 원도심 탐색의 리뷰·행사 카드, 관리자 행사 입력 화면은 각각 별도 지연 로딩 묶음으로 제공한다. 모바일 패널 높이는 상태 전환 때 한 번 확정하고 짧은 합성 변환만 적용한다. 베이스맵은 확대 중 소스를 바꾸지 않고 고화질 화면용 이미지 한 장을 유지한다. 공개 패널 이동은 History API 상태와 지도 선택 상태를 함께 관리한다.
