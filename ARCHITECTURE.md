# 프로젝트 구조

- `app/page.tsx`: 공개 지도, 관리자 편집기, 지도 조작, 장소 상세·후기·행사 UI의 중심 상태와 화면
- `app/public-place-detail-content.tsx`: 장소 선택 후에만 지연 로딩되는 공개 상세·행사·후기 화면
- `app/public-explorer-activity-content.tsx`: 원도심 탐색에서 리뷰·행사 탭을 열 때만 지연 로딩되는 카드 화면
- `app/globals.css`: 지도·패널·모바일 PWA 반응형 스타일
- `app/admin-*.tsx`: 지연 로딩되는 관리자 전용 화면
- `app/admin-place-event-dialog.tsx`: 관리자 행사 등록·수정 시에만 불러오는 행사 입력 창
- `app/api/`: 공개 배치, 장소 DB, 행사, 후기, 사진, 진단 기록 API
- `app/*density*.mjs`, `app/*placement*.mjs`: 라벨 밀집도·배치 계산
- `app/map-stage-transform.mjs`: PC·모바일별 지도 중심 정렬과 제스처 확대 변환 정책
- `app/mobile-render-budget.mjs`: 기기 성능에 따른 모바일 화면 밖 렌더 여유 범위 단계
- `app/mobile-marker-density.mjs`: 모바일 축척·성능 단계별 실제 마커·라벨 예산과 화면 중심 거리 기반 선택
- `public/`: 지도·랜드마크·마커 자산과 PWA manifest/service worker
- `db/`, `drizzle/`: 영구 저장 스키마와 마이그레이션
- `worker/`: 배포 Worker 진입점과 정적 자산 캐시 정책
- `tests/`: 기능·회귀·PWA·성능 정적 검증

관리자 PC 지도와 공개 지도는 조작 경로를 분리한다. 관리자 지도는 하나의 wrapper 변환에 React 상태의 이동·확대를 바로 반영하는 기존 직접 렌더 방식을 사용한다. 휠마다 현재 마우스 위치를 기준으로 확대하고, 장소 이동 시에도 고정 배율 1.55로 단순 중앙 정렬하므로 합성 렌더 전환·휠 기준점 고정·패널 안전영역 시점 보정을 거치지 않는다. 관리자 맞춤 배율은 가로·세로가 모두 보이는 기존 계산을 사용하고, 명시적으로 보이도록 설정한 라벨은 축척 예산으로 생략하지 않는다.

공개 지도는 제스처 중 합성 변형을 사용하고, 실제 이동이 시작되면 라벨 페인트를 잠시 중단한 뒤 종료 후 실제 확대율과 라벨 배치를 한 번 확정한다. 공개 PC는 지도판의 가운데 정렬을 레이아웃으로 고정하고 제스처 중 확대만 `scale3d`로 처리하며, 공개 모바일은 기존 가운데 이동을 포함한 변환 경로를 유지한다. 공개 PC 휠 확대는 입력 흐름이 끝날 때까지 최초 마우스 기준점을 고정한다. 공개 축소 하한은 가로로 긴 베이스맵의 좌우 끝이 화면 안쪽 여백에 맞는 배율로 계산하며 휠·핀치·버튼·단축키가 같은 하한을 사용한다. 라벨은 새 구성이 정착된 뒤 투명도 전환으로 한 번에 복구하고 상태표시는 장소명 바깥 상단 줄에 둔다. 공개 모바일 렌더 계층은 현재 화면의 기기별 여유 범위 안에서 축척·성능 단계만큼 실제 마커를 표시한다. 첫 진입 주요 거점 확대 수준에서는 축척 규칙상 후보 마커를 모두 유지한다. 맞춤 축척의 1.45배 이하 전경에서는 추천·주요 거점·선택 여부와 관계없이 일반 장소를 모두 화면 기준 8px 카테고리 원으로 통일하고 랜드마크 자산과 랜드마크 라벨만 유지한다. 간략 원은 지도 제스처의 역배율을 계층 단위로 적용해 화면상 작은 크기를 유지하며 0.75px의 어두운 회색 테두리를 사용한다. 간략 원·분류 점·공개 카테고리 UI는 승인 마커의 바깥 원 색상 한 묶음을 사용하며 음식점은 마커와 같은 `#E36B58`의 붉은색이다. 그보다 확대되면 추천 여부와 무관하게 화면 중심에 가까운 일반 장소부터 실제 마커 예산을 배정한다. 공개 라벨은 마커 예산과 별개로 관리하고, 지도 40% 이상이 보이는 맞춤 축척 2.5배 이하에서는 랜드마크·주요 거점·현재 선택 라벨을 항상 보존한 채 일반 라벨을 단계별 최대 0·2·4·6개만 추가한다. 이후 3·3.6·4.5배에서 일반 라벨을 18·34·68개로 늘리고 더 상세한 화면에서만 표시 대상으로 설정된 라벨 전체를 복구한다. 공개 개별 라벨 글자는 통합 라벨과 같은 8px 화면 크기로 고정하고 통합 라벨 연결선은 거리와 관계없이 최대 1.5px, 선택 연결선은 1.55px로 제한한다. 느린 정착 시간이 반복되면 현재 세션에서 저사양 범위와 마커·라벨 예산을 함께 적용한다. 장소 목록과 데이터 계산은 전체 공개 요소를 계속 사용하며, 선택 장소 상세, 원도심 탐색의 리뷰·행사 카드, 관리자 행사 입력 화면은 각각 별도 지연 로딩 묶음으로 제공한다. 공개 장소 상세는 권역을 노출하지 않고 DB의 설명·운영정보·비고/주의사항을 한 흐름의 정보 영역으로 표시한다. 원도심 탐색 상단은 전체 항목 수와 검색창을 같은 행에 두고 결과 안내를 얇은 보조 행으로 유지한다. 모바일 패널 높이는 상태 전환 때 한 번 확정하고 짧은 합성 변환만 적용한다. 베이스맵은 확대 중 소스를 바꾸지 않고 고화질 화면용 이미지 한 장을 유지한다. 공개 패널 이동은 History API 상태와 지도 선택 상태를 함께 관리한다.
