# 프로젝트 구조

- `app/page.tsx`: 공개 지도, 관리자 편집기, 지도 조작, 장소 상세·후기·행사 UI의 중심 상태와 화면
- `app/public-place-detail-content.tsx`: 장소 선택 후에만 지연 로딩되는 공개 상세·행사·후기 화면
- `app/public-explorer-activity-content.tsx`: 원도심 탐색에서 리뷰·행사 탭을 열 때만 지연 로딩되는 카드 화면
- `app/globals.css`: 지도·패널·모바일 PWA 반응형 스타일
- `app/admin-*.tsx`: 지연 로딩되는 관리자 전용 화면
- `app/admin-place-event-dialog.tsx`: 관리자 행사 등록·수정 시에만 불러오는 행사 입력 창
- `app/api/`: 공개 배치, 장소 DB, 행사, 후기, 사진, 진단 기록 API
- `app/*density*.mjs`, `app/*placement*.mjs`: 라벨 밀집도·배치 계산
- `app/dense-label-viewport.mjs`: 공개 화면의 현재 이동·축척을 지도 좌표 경계로 변환하고 통합 라벨 중심을 화면 안에 제한
- `app/map-stage-transform.mjs`: PC·모바일별 지도 중심 정렬과 제스처 확대 변환 정책
- `app/base-map-quality.mjs`: 저사양 모바일의 실제 표시 픽셀 수에 따른 4096 베이스맵 승격 기준
- `app/base-map-storage.ts`, `app/api/base-map/route.ts`: 현재 v20에만 버전이 고정된 화면용 2048·4096 WebP와 픽셀 동일 출력용 무손실 WebP를 연결하고, 이후 관리자 업로드 지도는 기존 R2 원본·파생본 경로 사용
- `app/landmark-assets/index.ts`: 고해상도 원본·512px 화면본·고정 384px 모바일 파생본의 자산 경로와 revision
- `scripts/extract-jfac-svg-assets.mjs`: 재단 공식 AI의 컬러 페이지를 Inkscape로 변환한 SVG에서 시그니처·심벌 경로만 추출하고 기존 화면 비율·헤더 팔레트를 보존하는 재현 스크립트
- `app/mobile-render-budget.mjs`: 기기 성능에 따른 모바일 화면 밖 렌더 여유 범위 단계
- `app/mobile-marker-density.mjs`: 모바일 맞춤 축척 2배의 일괄 마커 전환 기준과 축척·성능 단계별 라벨 예산
- `app/performance-diagnostics.mjs`: 지도 정착 진단 표본화와 느린 예외 보존 기준
- `public/`: 지도·랜드마크·마커 자산과 PWA manifest/service worker
- `public/landmarks-mobile-384/`: 공개 모바일이 최초 진입에서 한 번 선택해 세션 내 계속 사용하는 랜드마크 WebP 파생본
- `db/`, `drizzle/`: 영구 저장 스키마와 마이그레이션
- `worker/`: 배포 Worker 진입점과 정적 자산 캐시 정책
- `tests/`: 기능·회귀·PWA·성능 정적 검증

통합 라벨의 공개 연결선은 거리와 무관하게 불투명도 0.84를 유지한다. 관리자 한 열 통합 라벨은 항목명 길이로 폭을 정하며, 마커 왼쪽에 놓인 라벨은 항목명을 왼쪽 정렬하고 분류 표시점과 연결선 끝점을 행 오른쪽에 맞춘다.

관리자 PC 지도와 공개 지도는 조작 경로를 분리한다. 관리자 지도는 하나의 wrapper 변환에 React 상태의 이동·확대를 바로 반영하는 기존 직접 렌더 방식을 사용한다. 휠마다 현재 마우스 위치를 기준으로 확대하고, 장소 이동 시에도 고정 배율 1.55로 단순 중앙 정렬하므로 합성 렌더 전환·휠 기준점 고정·패널 안전영역 시점 보정을 거치지 않는다. 관리자 맞춤 배율은 가로·세로가 모두 보이는 기존 계산을 사용하고, 명시적으로 보이도록 설정한 라벨은 축척 예산으로 생략하지 않는다. 개별·통합 라벨에는 wrapper 확대의 역배율을 적용해 각각 9.5px·8.5px의 PC 화면 크기를 유지하고, 지도 이동·축척 변경 중에는 라벨과 통합 연결선을 페인트 경로에서 제외한 뒤 0.28초 투명도 전환으로 복구한다. 통합 라벨 연결선 끝점과 자동 배치 경계도 같은 고정 화면 크기 좌표계를 사용한다. 전체 라벨 새로고침은 저장된 통합 라벨 수동 위치를 비우고 이미지·개별 라벨·기존 연결선을 장애물로 평가하는 자동 배치를 다시 수행한다. 관리자 통합 라벨은 좌우 방향과 관계없이 한 열 세로 목록으로 표시한다. 라벨이 대상 마커의 왼쪽에 있으면 분류 표시점을 행 오른쪽에 두고 반대 방향은 왼쪽에 두며, 설정 화면은 현재 개별 라벨 수와 통합 묶음 수를 실시간으로 표시한다.

관리자 라벨 축척 예산은 기본적으로 비활성화해 기존 편집 가시성을 유지한다. 화면 설정에서 미리보기 체크를 켠 경우에만 공개본과 같은 저장된 축척 단계별 전체 라벨 목표 수를 적용하며, 하단 상태바는 관리자와 공개본 모두 현재 맞춤 대비 배율·지도 표시 비율·실제 출력 라벨 수를 표시한다.

축척별 전체 라벨 단계는 `map_label_density_settings`의 독립 런타임 설정으로 저장한다. 공개·관리자 초기 로드는 공개본이나 편집 초안의 과거 보기 설정보다 이 값을 우선 적용하므로, 라벨 단계 저장은 장소·좌표·자산 공개본 갱신과 공개 이력 생성을 요구하지 않는다. 설정 자체의 revision과 조건부 갱신으로 여러 관리자 화면의 동시 저장 충돌을 막는다.

화면용 라벨 후보는 인쇄 미리보기를 제외하고 확정된 지도 이동·배율에서 계산한 현재 화면 경계 안의 장소로 먼저 제한한 뒤 축척별 전체 라벨 예산과 밀집 통합을 적용한다. 이동·확대 중에는 이전 라벨을 숨긴다. 조작이 끝나면 다음 프레임에 라벨 구성·통합 라벨·연결선·EVENT/NEW 상태를 함께 갱신하고 0.12초 투명도 전환으로 복구한다. 관리자 편집 지도는 기존 0.28초 전환을 유지한다. 공개 하단의 새로고침은 네트워크나 베이스맵을 다시 읽지 않고 이 화면 경계와 라벨·마커 렌더 계층만 재계산한다.

지도 요소·DB 장소·장소 요청 마커는 데이터 변경 시 만든 인덱스를 선택·검수·공개 목록에서 재사용한다. 모바일 실제 마커와 간략 원은 한 번의 후보 순회로 나누고, 공개 장소 검색용 추가분류·편의정보 문자열도 장소 데이터가 바뀔 때만 계산한다. 관리자 장소 요청 카드 묶음은 해당 탭 진입 시 지연 로딩한다.

공개 지도는 제스처 중 합성 변형을 사용하고, 실제 이동이 시작되면 라벨 페인트를 잠시 중단한 뒤 종료 후 실제 확대율과 라벨 배치를 한 번 확정한다. 공개 PC는 지도판의 가운데 정렬을 레이아웃으로 고정하고 제스처 중 확대만 `scale3d`로 처리하며, 공개 모바일은 기존 가운데 이동을 포함한 변환 경로를 유지한다. 공개 PC 휠 확대는 입력 흐름이 끝날 때까지 최초 마우스 기준점을 고정한다. 공개 축소 하한은 가로로 긴 베이스맵의 좌우 끝이 화면 안쪽 여백에 맞는 배율로 계산하며 휠·핀치·버튼·단축키가 같은 하한을 사용한다. 라벨은 새 구성이 정착된 뒤 투명도 전환으로 한 번에 복구하고 상태표시는 장소명 바깥 상단 줄에 둔다. 통합 라벨은 조작 종료 시 현재 이동·축척의 보이는 지도 경계와 실제 화면상 라벨 폭을 다시 계산해 화면 안쪽으로 재배치하며, 모바일에서는 한 묶음 최대 10곳을 한 열 세로 목록으로 표시한다. 공개 모바일 렌더 계층은 현재 화면의 기기별 여유 범위 안의 요소만 유지하고, 저사양 단계는 화면 폭의 35% 또는 72px 중 큰 값을 여유 범위로 사용한다. 맞춤 축척 2배 미만에서는 추천·주요 거점·선택 여부와 관계없이 일반 장소를 모두 화면 기준 7.2px 카테고리 원으로 통일하고 랜드마크 자산과 랜드마크 라벨만 유지한다. 2배부터는 같은 화면 범위의 일반 장소를 모두 실제 마커로 한 번에 전환하며 기기 등급이나 화면 중심 거리에 따른 부분 표시를 사용하지 않는다. 간략 원은 지도 제스처의 역배율을 계층 단위로 적용해 화면상 작은 크기를 유지하며 0.75px의 어두운 회색 테두리를 사용한다. 간략 원·분류 점·공개 카테고리 UI는 승인 마커의 바깥 원 색상 한 묶음을 사용하며 음식점은 마커와 같은 `#E36B58`의 붉은색이다. 공개 라벨은 마커 전환과 별도의 축척·성능 예산으로 관리한다. 맞춤 축척 1.25·1.7·2.1·2.5·3·3.6·4.5배의 저장값은 랜드마크·주요 거점·현재 선택을 포함한 화면 전체 라벨 목표 수다. 필수 라벨이 목표 수보다 적으면 남은 수만 일반 라벨에 배정하고, 필수 라벨만 목표 수보다 많으면 필수 라벨은 모두 보존한다. 이 값은 독립 런타임 설정으로 저장되며 공개 이력과 무관하게 다음 공개 지도 로드부터 우선 적용된다. 4.5배를 넘는 상세 화면에서는 표시 대상으로 설정된 라벨 전체를 복구한다. 공개 개별 라벨 글자는 통합 라벨과 같은 8px 화면 크기로 고정하고 통합 라벨 연결선은 거리와 관계없이 최대 1.5px, 선택 연결선은 1.55px로 제한한다. 느린 정착 시간이 반복되면 현재 세션에서 저사양 범위와 라벨 예산을 함께 적용한다. 성능 진단은 시작 1회와 지도 이동·확대 5회 중 1회만 전송하되 200ms 이상 느린 정착은 항상 보존한다. 장소 목록과 데이터 계산은 전체 공개 요소를 계속 사용하며, 선택 장소 상세, 원도심 탐색의 리뷰·행사 카드, 관리자 행사 입력 화면·장소 요청 관리 화면은 각각 별도 지연 로딩 묶음으로 제공한다. 통합 라벨 배치 점수는 후보별 중간 배열과 전체 정렬 없이 단일 최솟값 순회로 계산한다. 공개 장소 상세는 권역을 노출하지 않고 DB의 설명·운영정보·비고/주의사항을 한 흐름의 정보 영역으로 표시한다. 원도심 탐색 상단은 전체 항목 수와 검색창을 같은 행에 두고 결과 안내를 얇은 보조 행으로 유지한다. 모바일 패널 높이는 상태 전환 때 한 번 확정하고 짧은 합성 변환만 적용한다. 저사양 모바일 베이스맵은 일반 배율에서 2048 화면용 이미지를 사용하고 예상 표시 픽셀이 1840px에 도달하면 4096 이미지를 미리 디코딩해 세션 중 한 번 승격한다. 표준·고사양 모바일과 PC는 기존 4096 화면용 이미지를 유지하며 인쇄·내보내기는 원본을 계속 사용한다. 공개 패널 이동은 History API 상태와 지도 선택 상태를 함께 관리한다.

공개 랜드마크는 모바일 여부를 첫 클라이언트 렌더에서 한 번 판정한다. 모바일은 `mobileSrc`의 384px WebP를 시작 로딩과 실제 마커에 동일하게 사용하고, 확대·축소 중 256·512px로 바꾸지 않는다. PC와 관리자 후보·편집 화면은 `screenSrc`의 512px 화면본을 유지하고 PNG 출력은 `src`의 고해상도 원본을 사용한다. 범용 마커는 작은 SVG를 그대로 유지한다. 카테고리 UI는 질감·음영이 있는 승인 일러스트라 무손실 WebP를 사용하고, 재단 국문 시그니처 B와 헤더 심벌은 공식 AI 원본의 곡선 경로를 추출한 SVG를 모든 웹·PWA 화면에서 공통 사용한다. 이 두 SVG는 revision query와 Worker의 immutable 캐시를 사용하며 런타임 해상도 교체를 하지 않는다. PWA 설치 아이콘은 manifest 호환을 위해 PNG를 유지한다.

현재 v20 베이스맵은 일반 지도 표시에서 기존 2048·4096 손실 WebP를 사용한다. 고화질 내보내기 때만 8,944×7,324 무손실 WebP를 불러와 캔버스에 합성하며, 최종 사용자가 받는 파일은 기존 8,944px 또는 12,000px PNG다. 이 고정 자산 연결은 현재 R2 원본의 업로드 버전이 `BUNDLED_V20_SOURCE_VERSION`과 일치할 때만 사용하므로 이후 관리자가 새 지도를 업로드하면 새 R2 원본과 그 화면 파생본으로 자동 복귀한다.
