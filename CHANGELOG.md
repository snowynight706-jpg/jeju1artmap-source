# 변경 기록

## 2026-08-21

- 배포본 통합 라벨 연결선을 거리와 무관한 고정 불투명도로 강화하고, 관리자 한 열 통합 라벨의 빈 폭과 좌측 배치 연결점 정렬을 교정
- 관리자 축척별 일반 라벨 설정의 기본값 버튼 옆에 서버 저장 버튼을 추가하고, 저장 충돌·실패 상태를 안내하도록 보강
- 공개 통합 라벨을 이동·축척 종료 후 현재 화면 경계에 맞춰 재배치하고 모바일 묶음을 최대 10곳의 한 열 목록으로 변경
- 관리자 통합 라벨을 한 열 목록으로 통일하고 좌측 배치의 표시점·글자·연결선 경계를 같은 화면 폭 기준으로 교정
- 관리자 화면·라벨 설정에 현재 개별 라벨 수와 통합 묶음 수 표시 추가
- 관리자 개별·통합 라벨을 축척과 무관한 PC 화면 크기로 복원하고 지도 이동·축척 변경 종료 후 페이드인하도록 변경
- 전체 라벨 새로고침 시 통합 라벨 수동 위치도 자동 재배치하며, 왼쪽에 놓인 통합 라벨 행은 분류 표시점을 오른쪽에 배치
- 관리자 화면에 배포본 축척 단계별 일반 라벨 상한 편집 UI를 추가하고 설정을 공개본 상태 기록·배포 이력에 포함
- 관리자 통합 라벨의 연결선 끝점 계산에서 공개본 전용 역배율을 분리해 확대 시 라벨 행과 선이 어긋나는 문제 수정
- 관리자 PC 지도를 기존 직접 이동·확대 방식으로 복원하고 합성 렌더 전환·고정 휠 기준점·패널 안전영역 시점 보정을 관리자 경로에서 제거
- 관리자 맞춤 축척을 가로·세로 기준 초기 계산으로 되돌리고 축척별 자동 라벨 생략을 제거
- 공개 장소 상세의 권역 표시를 제거하고 DB 설명·운영정보·비고/주의사항을 한 연속 정보 영역으로 통합
- 확대·축소 중 지도와 마커를 같은 합성 좌표계로 처리하고 휠 갱신을 일괄 확정하도록 개선
- 핀치 입력의 미세 떨림 완화와 관리자 PWA 성능 기록 추가
- 장소 상세·탐색 패널의 닫기 동작이 현재 지도 축척과 위치를 보존하도록 변경
- 주요 거점 확대와 최종 라벨 계산이 실제 반영된 뒤 로딩 화면이 닫히도록 순서 보강
- 부자연스러웠던 확대 종료 라벨 크기·페이드 보간 제거
- 장기 작업을 위한 프로젝트 구조·상태 문서 추가
- 관리자 지도가 첫 드래그 전 좌측 위에 있다가 중앙으로 점프하던 초기 중심 변환 누락 수정
- 모바일 PWA에서 주요 거점 맞춤 단계가 반올림된 지도 폭을 기다리며 끝나지 않던 로딩 조건을 실제 목표 폭 동기화 방식으로 보강
- 장소 이동 애니메이션의 매 프레임 지도 폭 변경을 제거하고 합성 변형 후 한 번만 실제 폭을 확정하도록 변경
- 핀치·휠 종료 시 지도 폭 확정과 라벨 밀집 계산을 분리하고 라벨 갱신을 낮은 우선순위로 전환
- PWA 캐시 이미지가 즉시 완료된 뒤 진행률 초기화가 실행되어 `0/3`으로 되돌아가던 순서 오류 수정
- 목표 확대·위치를 두 프레임 반영한 이후 중복 픽셀 비교가 모바일 로딩 종료를 차단하지 않도록 수정
- 드래그·확대·축소 중 개별·통합 라벨과 통합 연결선을 잠시 숨겨 지도 합성 렌더링 부담을 축소
- 조작 종료 시 라벨을 짧게 페이드인하고, 모바일 베이스맵의 2048·4096 소스 교체를 제거해 확대 중 깜박임 방지
- 공개 모바일 화면 밖 마커·통합 라벨은 넉넉한 여유 범위 밖에서만 렌더 DOM에서 제외
- EVENT·NEW·후기 수를 개별 라벨 상단 바깥 상태 줄로 옮겨 짧은 장소명 가림을 제거
- 지도 조작 종료 후 라벨 페이드인을 0.28초로 완화하고 공개 장소 상세·행사·후기 UI를 지연 로딩으로 분리
- 모바일 장소·탐색 패널의 높이 애니메이션을 제거하고 최종 높이 확정 뒤 0.24초 합성 안착 전환으로 변경
- 모바일 패널 배경 블러를 제거하고 주요 UI 모션 시간을 공통 기준으로 정리
- 원도심 탐색 리뷰·행사 카드 UI를 탭 선택 시 지연 로딩되는 별도 묶음으로 분리하고 0.16초 교차 표시 추가
- PC 지도판의 가운데 정렬과 확대 변환을 분리해 드래그·축척 중 마커와 랜드마크의 좌우 소수점 흔들림 완화
- 모바일 화면 밖 마커·통합 라벨 여유 범위를 기기 메모리·코어 수에 따라 조정하고 느린 정착이 반복되면 자동으로 저사양 범위 적용
- 모바일 웹·PWA에서 축척·기기 성능에 따라 실제 마커·라벨 수를 제한하고 추천 장소 우선 표시, 생략 장소는 카테고리 색상 원으로 대체
- 랜드마크를 모바일 화면 밖·개수 제한에서 제외하고 확대 종료 후 라벨 구성이 한 번에 복구되도록 타이밍 정리
- PWA 최상위 차콜 배경을 밝은 화면 배경으로 교체하되 상단 안전영역 색상은 유지해 합성 재구성 중 전체 암전 방지
- 관리자 행사 등록·수정 UI를 지연 로딩 묶음으로 분리해 공개 초기 스크립트 부담 축소
- 모바일 생략 마커의 카테고리 원을 12px로 확대하고 터치 영역을 함께 확장
- 첫 진입 확대 수준은 화면 주변 마커를 모두 표시하되 전경 축척의 기기별 마커·라벨 예산은 더 단순하게 조정
- 전체 마커 표시 구간의 불필요한 추천·거리 정렬을 생략해 동일한 표시 품질로 정착 계산량 축소
- 모바일 전경에서는 추천 장소도 일반 장소와 같은 카테고리 원으로 통일하고 랜드마크 라벨만 유지하도록 단순화
- 모바일 간략 원을 85% 기준 크기로 줄이고 축척 비례 크기, 단일 검정 테두리, 음식점·카페 명도·채도 분리 색상 적용
- 모바일 전경 간략화 구간에서는 추천·주요 거점·선택 여부를 무시하고 랜드마크가 아닌 모든 장소를 원으로 통일
- 모바일 간략 원을 화면 기준 7px 고정 크기로 축소하고 제스처 역배율을 계층 단위로 적용해 확대 중 크기 증가 방지
- 관리자 휠 확대의 최초 마우스 기준점을 입력 종료까지 고정해 축척 중심 흔들림 완화
- 배포본 원도심 탐색의 전체 항목 수와 검색창을 한 줄로 통합하고 결과·카테고리 여백을 줄여 목록 상향 배치
- 승인 마커의 바깥 원 색상을 카테고리 색상 단일 원본으로 통합하고 음식점 UI·간략 원을 마커와 같은 붉은색 `#E36B58`로 교정
- 모바일 간략 원을 화면 기준 8px로 소폭 확대하면서 테두리는 0.75px 어두운 회색으로 완화
- 추천 여부를 화면 렌더 우선순위에서 제거하고 전경부터 상세까지 축척·화면 중심 거리 기준으로 마커와 라벨을 단계적으로 복구
- 맞춤 축척의 2.2배를 넘는 상세 화면에서는 명시적 표시 대상 마커·라벨의 자동 개수 제한을 해제하고 최초 화면 전용 예외를 제거
- 베이스맵 좌우 끝이 화면에 맞는 배율을 공통 축소 하한으로 적용해 휠·핀치·버튼·단축키의 과도한 축소를 차단
